# translations

Automatisch erzeugte i18n-Wörterbücher je Screen/Namespace (Deutsch + English).
Jede Datei `<namespace>.ts` exportiert `de` und `en` (Key → Text).
Eingebunden und zusammengeführt in `app/lib/i18n.ts`.
